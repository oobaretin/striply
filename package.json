{
  "name": "striply",
  "version": "1.0.0",
  "description": "Striply — Local-first tools for diabetic test strip resale (browser storage)",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  },
  "engines": {
    "node": "20.x"
  }
}
